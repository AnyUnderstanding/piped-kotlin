pi main() : Int {
    (1, 4) |> add |> (#0, 3) |> printTuple |> id |> (#0) |> id |> print;
    >> 0;
}

pi add(a : Int, b : Int) : Int {
    >> a + b;
}

pi printTuple(a : Int, b : Int) : Int {
    (a) |> print;
    (b) |> print;
    >> 0;
}

pi id(elem : Int) : Int {
   >> elem;
}