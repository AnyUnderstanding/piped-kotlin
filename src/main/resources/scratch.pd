pi main() : Int {
    let x: Int = 1;
    let r: Int = 4;
    let m : Int = (x, r) |> add [ (l) |>
        (l==1) -> 2,
        else -> 3
    ];
    >> m;
}

pi add(x: Int, y: Int) : Int {
    >> x + y;
}




